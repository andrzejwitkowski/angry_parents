import { MongoRegistrationProcessRepository } from '../src/adapters/secondary/MongoRegistrationProcessRepository'; import mongoose from 'mongoose'; import { RegistrationProcessSchema } from '../src/models/RegistrationProcess'; await mongoose.connect(process.env.MONGODB_URI || 'mongodb://127.0.0.1:27017/angry_parents'); const repo = new MongoRegistrationProcessRepository(); const processes = await repo.findAll(); console.log(JSON.stringify(processes[0])); process.exit(0);
